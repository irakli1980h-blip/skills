---
"mattpocock-skills": patch
---

Tighten 26 catalog descriptions to a single short clause and move the trigger branches they carried into a `## When to reach for it` section in each skill body, with a pointer to the sibling skill where that one is the better fit. The model-facing catalog stays small while the routing cues survive, and harnesses that ignore `disable-model-invocation` (Hermes Agent, for example) get body-level guidance for the user-invoked skills instead of none. No behaviour, name, or invocation-flag changes.
